/**
 * This package contains adapter controllers that map frontend API expectations to backend implementations.
 * 
 * These adapters serve as a bridge between the frontend service calls and the actual backend controllers,
 * allowing the frontend to use its expected API paths and formats while the backend maintains its own
 * implementation structure.
 */
package com.example.senti_mate_back_end.adapter;